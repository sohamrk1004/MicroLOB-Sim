[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "microlob-sim"
version = "0.1.0"
description = "A deterministic Limit Order Book matching engine built from first principles."
authors = [{ name = "Soham Kulkarni" }]
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
